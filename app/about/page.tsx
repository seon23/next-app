import DateTime from '../ui/DateTime';

export default function AboutPage() {
  // if (true) throw new Error('Error in About Page');
  return (
    <div>
      <h1>This is About Page</h1>
      <DateTime />
    </div>
  );
}
